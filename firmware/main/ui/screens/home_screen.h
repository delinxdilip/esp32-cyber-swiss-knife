#pragma once

#include "ui/ui_model.h"

// ------------------------------------------------------------
// HOME SCREEN
// ------------------------------------------------------------

class HomeScreen
{
public:
    /**
     * Render the Home screen.
     *
     * @param model Current UI data model.
     */
    static void render(
        const UIModel &model);
};